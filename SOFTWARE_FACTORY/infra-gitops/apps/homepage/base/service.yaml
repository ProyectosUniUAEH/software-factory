apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: prod
  labels:
    app: homepage
spec:
  selector:
    app: homepage
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
