apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: constructora
  namespace: prod
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - constructora.futurefarms.mx
      secretName: constructora-tls
  rules:
    - host: constructora.futurefarms.mx
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: constructora
                port:
                  number: 80
