apiVersion: v1
kind: Service
metadata:
  name: fago-api
  namespace: prod
  labels:
    app: fago-api
spec:
  selector:
    app: fago-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
