apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homepage
  namespace: prod
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - homepage.futurefarms.mx
      secretName: homepage-tls
  rules:
    - host: homepage.futurefarms.mx
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: homepage
                port:
                  number: 80
