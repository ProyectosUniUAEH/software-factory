apiVersion: v1
kind: Service
metadata:
  name: constructora
  namespace: prod
  labels:
    app: constructora
spec:
  selector:
    app: constructora
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
