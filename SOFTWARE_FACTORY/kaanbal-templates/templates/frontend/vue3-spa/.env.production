# `npm run build` (el que corre el pipeline): la API de este stack.
# Kaanbal escribió esta URL al crear el repositorio. Si la API cambia de
# dominio, cambia esta línea y haz push: la imagen se reconstruye con la nueva.
VITE_API_URL={{ API_URL }}
