apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - rbac.yaml
  - bootstrap-admin-job.yaml
# @kaanbal:if API_PUBLIC
  - ingress.yaml
# @kaanbal:endif
