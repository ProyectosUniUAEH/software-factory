apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: prod

commonLabels:
  environment: prod

patchesStrategicMerge:
  - patch-deployment-labels.yaml

# El tag lo fija el instalador en el bootstrap y a partir de ahí lo actualiza
# el workflow de GitHub Actions del repo kaanbal-api en cada push a main.
images:
  - name: kaanbal-api-image
    newName: ${KAANBAL_DOCKER_USER}/kaanbal-api
    newTag: ${KAANBAL_API_TAG}
