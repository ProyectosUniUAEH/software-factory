# Credentials for kaanbal-api (Git, Bitbucket, etc.)
# In production, use sealed-secrets or external-secrets
apiVersion: v1
kind: Secret
metadata:
  name: kaanbal-credentials
  labels:
    app: kaanbal-api
type: Opaque
stringData:
  git-username: "futurefarms-softwarefactory"
  git-token: "REPLACE_WITH_SEALED_SECRET"
  dockerhub-username: "andresupmh"
  dockerhub-token: "REPLACE_WITH_SEALED_SECRET"
