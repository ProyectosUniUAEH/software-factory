apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: tailscale

# Keep the operator aligned with the vendored CRDs and proxy image.
images:
  - name: tailscale/k8s-operator
    newTag: v1.102.3
