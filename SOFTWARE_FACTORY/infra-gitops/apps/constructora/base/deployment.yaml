apiVersion: apps/v1
kind: Deployment
metadata:
  name: constructora
  namespace: prod
  labels:
    app: constructora
spec:
  replicas: 1
  selector:
    matchLabels:
      app: constructora
  template:
    metadata:
      labels:
        app: constructora
    spec:
      containers:
        - name: app
          image: nginx:stable-alpine
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
